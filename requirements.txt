# === Core Libraries ===
requests==2.32.3             # HTTP requests, foundational for web interaction
httpx==0.28.1                # Async alternative to `requests`, used in modern clients
urllib3==2.4.0               # Low-level HTTP client used by requests
tqdm==4.67.1                 # Progress bars for CLI tools

# === GitHub API Integration ===
PyGithub==2.6.1              # GitHub API wrapper

# === OpenAI Integration ===
openai==1.75.0               # Official OpenAI Python SDK

# === Data Models & Validation ===
pydantic==2.11.3             # Typed data classes and validation
pydantic_core==2.33.1        # Low-level validation engine used by Pydantic
annotated-types==0.7.0       # Extension for typing annotations
typing-inspection==0.4.0     # Utilities for inspecting Python type hints
typing-extensions==4.13.2    # Backports of newer typing features
typing==3.7.4.3              # Required for compatibility with older codebases

# === Environment & Configuration ===
python-dotenv==1.1.0         # Load `.env` files into environment variables
dotenv==0.9.9                # Legacy dotenv support (can be removed if unused)

# === Networking & Transport ===
anyio==4.9.0                 # Async concurrency backend (used by httpx)
sniffio==1.3.1               # Detect current async library (used by anyio)
httpcore==1.0.8              # Core transport layer for httpx
h11==0.14.0                  # HTTP/1.1 protocol used by httpcore

# === Security & Authentication ===
cryptography==44.0.2         # Encryption primitives, certificate handling
cffi==1.17.1                 # C bindings interface, dependency of cryptography
pycparser==2.22              # C parser required by cffi
PyNaCl==1.5.0                # Encryption library used in GitHub authentication
PyJWT==2.10.1                # JSON Web Tokens for secure API access

# === Utilities ===
wrapt==1.17.2                # Function decorators and wrappers
Deprecated==1.2.18           # Mark functions/classes as deprecated
distro==1.9.0                # Linux distribution information
charset-normalizer==3.4.1    # Fallback for encoding detection (used by requests)
idna==3.10                   # Internationalized domain name support

# === Optional / Experimental ===
jiter==0.9.0                 # Lightweight and fast JSON serialization
